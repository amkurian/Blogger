<p>Article Title:<strong><%= @article.title %></strong></p>
<p>Article Text:<strong><%= @article.text %></strong></p>
<h2>Comments</h2>
<% @article.comments.each do |comment| %>
<p>Commenter:<strong><%= comment.commenter %></strong></p>
<p>Comment:<strong><%= comment.body %></strong></p>
<% end %>


<h2>Add New Comment</h2>
<%= form_for([@article,@article.comments.build]) do |f| %>
<p>
<%= f.label :commenter %>
<%= f.text_field :commenter %>
</p>
<p>
<%= f.label :body %>
<%= f.text_area :body %>
</p>
 <p>
<%= f.submit %>
 </p>
 <% end %>
<%= link_to 'Create new',new_article_path %>